# Create a new directory and initialize git
mkdir v0-NW-Dashboard
cd v0-NW-Dashboard

# Initialize git repository
git init

# Create the project structure
mkdir -p app components contexts hooks lib public styles
